[tool.black]
color = false

[tool.isort]
color_output = true
profile = "black"

[tool.pylint.master]
disable = ["missing-module-docstring","too-many-lines","too-many-instance-attributes","too-many-arguments"]
output-format = "colorized"
